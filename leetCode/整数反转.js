/*
* 给出一个32位的有符号整数,你需要将这个整数中每位上的数字进行反转
* 思路: 1.先把符号去掉,按正整数处理
*      2.把数字转成字符串,反转字符串
*      3.因为最大合法数字是10位数,正值为2147483647,负值为2147483648,所以大于10位的直接返回0;小于10位的一定没问题;等于10位的
* 直接按字符串比较大小
 */
let reverse = function (x) {
  let fn = "", re;
  if (x<0) {
    fh = "_";
    x = 0 - x;
  }
  re = (x + "").split("").reverse().join("");
  if (re.length > 10 || re.length === 10 && re > (x < 0 ? "2147483648" : "2147483647")) {
    return 0;
  } else {
    return fh + re;
  }
};
